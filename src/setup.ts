import {setUpDesignDocuments} from './util/setup'

export function setup(database:any, callback:any) {
    //setUpDesignDocuments(database, callback);
    console.log('Woop design documents created')
}